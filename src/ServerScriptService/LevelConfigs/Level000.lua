local module = {}

function module.getTargetWords()
    return {
        {
            {word = 'RAT', target = 1, found = 0},
            {word = 'BAT', target = 1, found = 0}
        }
    }
end

return module
